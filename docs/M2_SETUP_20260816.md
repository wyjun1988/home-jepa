# M2 Max 이주 완료 (2026-08-16)

주 개발기를 iMac(x86_64, torch 2.2.2, fp32 고정) → **MacBook Pro M2 Max(arm64)** 로 옮겼다.

## 환경

| | iMac (구) | **M2 Max (현)** |
|---|---|---|
| 아키텍처 | x86_64 | **arm64** |
| torch / numpy | 2.2.2 / 1.26.4 (휠 한계로 고정) | **2.8.0 / 2.0.2** |
| bf16 autocast | ❌ 불가 | ✅ **가능** (미사용 — 필요시 활성화) |
| 학습 속도 (bs128, d128 L4) | 956 ms/step | **299 ms/step (3.2배)** |
| VRAM | 8GB (영상 재생과 충돌) | 통합 메모리 32GB |

venv: `~/work/home-jepa/.venv` (**전용** — stock-v2 venv 는 건드리지 않는다).
`python3 -m venv .venv && .venv/bin/pip install torch numpy pandas`

## 이주 중 고친 것 — torch 2.6+ 호환

`torch.load` 의 `weights_only` 기본값이 2.6 에서 True 로 바뀌어, args 딕셔너리를 담은 우리
체크포인트가 **전부 로드 실패**한다. 10개 호출 전부에 `weights_only=False` 를 명시했다.
(우리 체크포인트는 자기 생성물이므로 안전.)

## 검증 (이주 판정)

동일 체크포인트·동일 데이터로 아이맥 수치를 **완전 재현**:

| | 아이맥 | M2 |
|---|---|---|
| test NLL | 0.606 | **0.606** |
| all top-1 | 0.860 | **0.860** |
| moved top-1 | 0.418 | **0.418** |
| 동거인 top-1 | 0.167 | **0.167** |

데이터 감사: `data/v5` PASS. 파일 동기: rsync 전송 0건(완전 일치).

## 이것이 바꾸는 것

3.2배 가속으로 **4090 에 넘겼던 Job 1(시드 매트릭스 8런)이 로컬에서 가능**해졌다:
5000스텝 ≈ 25분 → 8런 ≈ 4~5시간. 4090 대기 없이 P13 판정을 끝낼 수 있다.

## M2 자립 구성 (2026-08-16 완료)

M2 가 **주 개발기**다. 아이맥 없이 단독으로 전 사이클(생성→감사→학습→평가→커밋→푸시)이 돈다.

| 항목 | 상태 |
|---|---|
| 데이터 | `data/` 57GB (ADT 50G·HOMER+ 4.3G·시뮬 v0~v5) — 아이맥과 rsync 전송 0건 |
| 산출물 | `results/` 443MB, 체크포인트 66개 |
| 실행 | `~/work/home-jepa/.venv` (torch 2.8/numpy 2.0/arm64), 모듈 9개 import OK |
| git | 저장소·remote(`git@github-homejepa:`)·배포키·신원 전부 설정, **푸시 검증됨** |

정리한 것: 이주 중 rsync 실수로 `docs/*.md` 23개가 저장소 루트에 복제됐던 것 삭제, `.venv/` gitignore.

작업 지시는 `RUNBOOK_4090_20260812.md` 의 Job1 을 `scripts/p14_seeds_m2.sh` 가 대체한다
(4090 불필요 — M2 3.2배 가속으로 로컬 수행). Job3(스케일 곡선)도 동일하게 M2 에서 가능하다.
