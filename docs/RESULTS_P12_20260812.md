# P12 결과 — 조합 어트리뷰트 ON/OFF · JEPA 트랙 최종 판정 (2026-08-12)

두 미결을 동시에 판정: (a) 위치별 조합 어트리뷰트(`--room-feats`)가 값을 하는가,
(b) 조건을 맞춘 상태에서 JEPA 사전학습이 값을 하는가. 6개 모델을 같은 v5 데이터·같은 보조신호로 비교.

배경: P11 에서 두 침묵 결함을 발견했다 — ① JEPA stage-2 가 보조신호(aux+gate)를 못 받고 있었고
② `room_feats` 플래그가 꺼져 있어 **위치별 어트리뷰트 8종이 아예 모델에 안 들어가고 있었다**.
둘 다 고친 뒤의 결과가 아래다.

## 1. 결과표 (시뮬 test 5,760 질의, top-1/top-2)

| 버킷 | supervised_rf | jepa_rf_ft | **jepa_rf_scr** | supervised(OFF) | jepa_ft(OFF) | jepa_scr(OFF) |
|---|---|---|---|---|---|---|
| 전체 | 0.836/0.901 | **0.859/0.917** | 0.836/0.903 | 0.860/0.916 | 0.856/0.908 | 0.859/0.916 |
| 안 움직임 | 0.927 | **0.961** | 0.923 | 0.958 | 0.966 | 0.960 |
| 움직임 | 0.429 | 0.400 | **0.448** | 0.418 | 0.361 | 0.403 |
| 방 이동 | 0.424 | 0.402 | **0.450** | 0.422 | 0.352 | 0.403 |
| 방내 이동 | **0.454** | 0.391 | 0.437 | 0.397 | 0.402 | 0.402 |
| 내가 옮김 | **0.931** | 0.851 | 0.918 | 0.865 | 0.785 | 0.849 |
| 동거인이 옮김 | 0.148 | 0.146 | **0.183** | 0.167 | 0.122 | 0.152 |
| 전체 NLL | 0.649 | **0.592** | 0.651 | 0.606 | 0.624 | 0.591 |

## 2. 판정 A — 조합 어트리뷰트: **이동에는 도움, 전체에는 손해**

`room_feats` ON 은 일관된 트레이드오프를 만든다(supervised_rf vs supervised OFF):
- 이득: 방내 이동 0.397→**0.454**(+14%), 내가 옮김 0.865→**0.931**(+8%), 움직임 전체 0.418→0.429
- 손실: **안 움직임 0.958→0.927**, 전체 NLL 0.606→0.649

원인: 위치별 부재·조합 신호가 위치 헤드에는 유용하지만 **게이트로도 흘러 "뭔가 바뀌었다"를 과다
판정**한다. 질의의 82%가 stayed 이므로 이 손실이 이득을 덮는다. P8 적응 실패(오버샘플이 게이트
기저율을 파괴)와 정확히 같은 구조.

**조치 방향(미실행)**: 어트리뷰트를 목적지 경로에만 연결하고 게이트로 가는 그래디언트를 차단.
그러면 방내 이동·자가이동 이득만 취하고 stayed 손실을 피한다. P8 에서 같은 처방(`--dest-only`)이
통했다.

## 3. 판정 B — JEPA 트랙: **사전학습 이득이 재현되지 않는다 (시드 1, 강한 잠정)**

조건을 완전히 맞춘 3자 비교에서 **랜덤 초기화가 사전학습을 이긴다.** 두 설정 모두에서 재현:

| | 사전학습(ft) | 랜덤초기화(scr) | 차이 |
|---|---|---|---|
| ON: 움직임 / 동거인 / NLL | 0.400 / 0.146 / 0.592 | **0.448 / 0.183 / 0.651** | scr 우세(NLL 만 ft) |
| OFF: 움직임 / 동거인 / NLL | 0.361 / 0.122 / 0.624 | **0.403 / 0.152 / 0.591** | scr 전면 우세 |

사전학습이 이기는 축은 **안 움직임/NLL/전체**다 — 주의 깊게 보면 §1 표에서 **전체 top-1/top-2/NLL
최고는 jepa_rf_ft(0.859/0.917/0.592)** 인데, 이는 82% stayed 버킷의 우위가 만든 것이다. 이 프로젝트의
목표 지표(이동·동거인 케이스)에서는 랜덤초기화가 우세하므로 **Track J 는 주력에서 내린다.**

통계적 신뢰(2026-08-12 검토에서 추가): 시드 1개다. 이동 격차는 ON 4.8pt(≈2.2σ)·OFF 4.2pt(≈2.0σ),
동거인 3.7pt(≈1.8σ) — 개별로는 경계적이나 **두 설정에서 방향이 일관**된 것이 보강 근거다.
"확정"이 아니라 **강한 잠정**이 정직한 표현이며, 자기 교훈("결론은 시드 반복 후에만")에 따라
시드 2–3개 반복이 남은 확인 절차다.

### 이 판정이 이전 결론을 어떻게 바꾸는가 (정직하게)

P3–P8 에서 JEPA 우위로 보고한 결과들(P4 전이 우위, P5 스코어카드, P7·P8 연관 능력)은
**JEPA 가 보조신호를 못 받는 불리한 조건에서 나온 것**이라 방향이 반대였다 — 즉 그때 이겼던 것은
더 강한 결론이다. 그러나 조건을 맞추자 **같은 우위가 재현되지 않았다.** 두 사실을 종합하면:
"JEPA 사전학습의 이득으로 보였던 것의 상당 부분은 보조신호 부재를 사전학습이 부분적으로
보상한 것"이라는 설명이 가장 단순하다. **latent 사전학습 자체의 순이득은 이 규모/난이도에서
확인되지 않는다.**

남는 가치: frozen latent 멀티태스크 스코어카드(P5 §4)는 여전히 유효하다 — 하나의 표현이 7개
다운스트림을 받친다는 결과는 사전학습 방식과 독립이며, 랜덤초기화 트렁크로 재측정할 가치가 있다.

## 4. HOMER 전이 (supervised_rf)

| | zero-shot | 적응 후(+recal) |
|---|---|---|
| 전체 top-2 | 0.914 | 0.912 |
| **방 이동** | 0.263 | **0.330** (탐색 14.2→8.8 표면) |
| 방내 이동 | 0.134 | 0.084 |

방 이동은 적응이 계속 이득(+25%), 방내는 계속 손해 — P10 과 같은 패턴. 다만 P10 의
`two_head_v5`(어트리뷰트 OFF) 조합이 방 이동 0.414 로 더 좋았다: **실데이터 전이에서도
어트리뷰트 ON 은 손해.**

## 5. 결론 — 현재 최고 구성

**`two_head_v5`(어트리뷰트 OFF, 게이트 감독 ON, noid, L1 사람) + 현장 방-이동 한정 유사라벨 적응.**
HOMER 방 이동 top-2 0.414 / 탐색 6.4 표면. P12 의 두 후보(어트리뷰트 ON, JEPA 사전학습)는
모두 이 구성을 넘지 못했다.

## 6. 다음

1. **어트리뷰트를 목적지 경로 전용으로** — 유일하게 남은 유망 후보(§2 조치 방향).
2. **시드 반복(±2)으로 판정 B 를 확정** — 현재 시드 1. 
3. 스코어카드를 랜덤초기화 트렁크로 재측정 — 멀티태스크 주장의 사전학습 의존성 제거.
4. Track J 는 보존하되 주력에서 제외. 재도전한다면 **더 큰 모델/더 어려운 세계**에서.

## 7. 전체 검토 (2026-08-12, 사용자 지시 "모든 내용 검토")

**수치 대조**: P8–P12·README·CAPABILITY 헤드라인 44건을 결과 JSON 과 자동 대조 — 전부 일치
(1건은 recal 값 표기 차이로 판명, 표 헤더와 정합).

**발견·수정 6건**:
1. (치명) `data/packs/pilot_packs.json` 이 .gitignore 의 `data/` 에 삼켜져 **git 에 없었음** —
   클론 후 시뮬 재생성이 첫 명령에서 실패하는 상태. 예외 규칙으로 포함.
2. (치명) DATA_SETUP 의 베이스라인 통계 스니펫이 클린 클론(results/ 부재)에서 실패 — makedirs 추가.
3. compare.py 세대 매니페스트에 v5 계열 규칙 부재 → 기본 뷰가 "unknown" 으로 오염.
   일회성 인라인이던 매니페스트 생성을 `scripts/gen_manifest.py` 로 승격(재발 방지).
4. train_supervised 의 게이트 감독 경로가 인코더를 2회 통과(both+parts) — `all_heads`(1회)로
   통합, 기존 경로와 수치 완전 일치 검증. v5 계열 학습이 ~2배 빨라짐(결과 불변).
5. 문서 모순: README "핵심 결과"가 P5 시점 JEPA 우위를 현행처럼 서술 / 뒤집힘 로그에 P12 누락 /
   레시피 표의 JEPA 행 미갱신 / CAPABILITY 의 "JEPA 판 우위" 스테일 — 전부 시점 명시로 정합화.
6. 판정 B 의 "확정" 이 자기 교훈(시드 반복 후에만) 위반 — 통계 명시(격차 2σ 안팎, 두 설정 방향
   일관)와 함께 **강한 잠정**으로 정정, 시드 반복을 다음 절차로 등재.

**검증 후 이상 없음 확인**: ADT 다운로더의 출력 구조(디렉터리명·flat CSV)가 기존 수동 추출과
일치 · noid 앵커 구현과 주석 정합화 · git 에 데이터/체크포인트 누출 0건 · 모듈 9개 import ·
스크립트 21개 문법 · 세 데이터셋 감사 PASS(직전 점검).
