# P7 결과 — 지각 리얼리즘: re-id 제거(noid) · 조합 부재판정 · LLM 팩 (2026-08-09 밤)

사용자 설계 3건 반영: ① LLM 은 행동 레이어(가구 팩), 이벤트 생성 아님 ② re-id 제거 — 연관은
모델 몫 ③ 장소·부재는 물체 조합으로(→ 어트리뷰트 증류, §11.6). 전 데이터 감사 PASS
(v4 재생성 시 감사가 시뮬 레이스 1건 적발·수정 — carry-attach 가 미래시각 tidy 의 obj_cur 를 읽던 것).

## 1. LLM 팩 파일럿(v3) — 절반의 대성공

세션-저작 12가구 팩(Qwen few-shot 예시 겸용): **zero-shot 캘리브레이션 역대 최고**
(HOMER stayed NLL 0.087, recal 없이 last_known 동률) — 팩 다양성이 prior 전이를 원천 해결.
단 moved 목적지는 후퇴(top-2 0.094 vs r2 0.112) — 팩의 after 분포가 생활사 체인을 희석.

## 2. re-id 제거의 비용과 역설적 이득 (v4 시뮬, 동일 데이터 A/B)

| | v4id | v4no(noid) |
|---|---|---|
| 전체 top-1 | 0.887 | 0.843 (−4.4pt = 연관 프리미엄) |
| moved_within | 0.454 | **0.046** (최대 피해 — 방내 동클래스 이동) |
| **동거인이 가져감** | 0.097 / t2 0.355 | **0.139 / t2 0.392 (역전 우위)** |

**대표 시나리오(동생이 노트북 가져감)에서 noid 가 오히려 이긴다** — 인스턴스 장부 대신
클래스 풀링+조합 어트리뷰트(f_miss/f_aff)가 그 케이스의 본질과 정합.

## 3. 연관 능력 층화 (단일 vs 다중 인스턴스, moved)

| top-1 / top-2 | 단일(223) | 다중(825) |
|---|---|---|
| v4id | 0.619 / 0.762 | 0.368 / 0.539 |
| two_head_v4no | 0.175 / 0.529 | 0.234 / 0.467 |
| **jepa_v4no_ft** | **0.341 / 0.673** | **0.278 / 0.507** |

- **JEPA 사전학습이 noid 연관을 크게 돕는다**(supervised noid 대비 전 층 우위) — JEPA 테제의 새 증거.
- 단일 인스턴스에서 noid 의 급락(0.619→0.175)은 파이프라인 슬랙: 단일 클래스는 논리적으로 연관이
  확정인데 못 살림. 원인 후보 = noid 앵커가 SELF_DROP 을 버림(자기 손 identity 는 현실적으로 유효),
  학습 스텝 부족. **개선 여지 명확.**

## 4. HOMER 전이 — 블렌드 실패 (정직)

| moved top-2 | r2 손코딩체인 | r3 팩 | v4id 블렌드 | v4no |
|---|---|---|---|---|
| | **0.112** | 0.094 | 0.084 | 0.097 |

블렌드(활동 after 50%+생활사 체인 50%)는 목적지·캘리브레이션 모두 중간치로 수렴 — 손코딩 체인의
날카로운 전이 패턴이 여전히 목적지 최강. **다음 수(원리적 해법): 목적지 체인을 저작하지 말고
대상 로그의 재목격 통계에서 라벨-프리로 추정** — 보류 중인 재관측 유사라벨 적응과 합류.
ADT(실센서): v4no 전체 t2 0.977 / NLL 0.225 로 안착(moved n=27 참고치).

## 5. P7 판정 요약

성립: noid 전환(연관은 모델 몫) + 조합 부재판정 어트리뷰트 + 동거인 시나리오 역전 우위 +
JEPA 사전학습의 연관 기여 + LLM 팩의 캘리브레이션 해결. 미해결: 방내 동클래스 연관(0.046),
목적지 체인의 전이(0.112 벽), 단일 인스턴스 슬랙.

**다음 우선순위**: ① noid 슬랙 회수(SELF identity 복원=에피소드 내 자기 손, 스텝 증가)
② 재목격 유사라벨로 목적지/readout 적응(라벨-프리, 0.112 벽 정면) ③ Qwen 로컬 팩 대량화
(캘리브레이션 축은 이미 승리 — 다양성 스케일) ④ 클래스 오인식 노이즈.
